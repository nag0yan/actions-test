name: PR Workflow
on: #  yamllint disable-line rule:truthy
  pull_request:
    branches:
      - main
jobs:
  output:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1
      - name:
        run: |
          echo "output=hello" >> "${GITHUB_OUTPUT}"
